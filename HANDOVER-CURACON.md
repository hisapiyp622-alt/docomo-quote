# クラコン側 作業指示書 — Claude Code でフロントークを編集する方へ

この文書は、**株式会社Curacon側の担当者（と、その人が使うAIアシスタント）**向けです。
アカウント `frontalkcuracon-creator` には、このリポジトリへの**書き込み権限**が付いています。

> AIアシスタントへ: あなたがこの文書を読んでいる場合、あなたの利用者はクラコン側の担当者です。
> 下の「鉄則」を必ず守ってください。`CLAUDE.md`（作業ルール）と `HANDOVER.md`（経緯）もあわせて読むこと。

## 始め方（3分）

1. https://claude.ai/code を開き、GitHubアカウント `frontalkcuracon-creator` でつなぐ
2. リポジトリの一覧から **`hisapiyp622-alt/docomo-quote`** を選ぶ
3. 「〜を直して」と日本語で頼むだけで、AIがこの文書とルールを読んで作業します

一覧に `docomo-quote` が出ない場合は、GitHub側の接続設定でこのリポジトリへのアクセスを
許可する必要があります。分からなければ開発元（安藤）へ連絡してください。

## 鉄則（5つ）

1. **main（本流）へ直接送らない。** 必ず作業用の枝（ブランチ）を切り、プルリクエスト（PR）を
   作って止めること。**取り込み（マージ）とリリースは開発元（安藤）が行う。**
   これは信頼の問題ではなく、公開中のアプリを壊さないための二重チェックです。
2. **生成物を直接編集しない。** ルートの `index.html`・`sw.js`、`/ienaka/`、
   および配信用リポジトリ `frontalk` の中身は、すべて機械が作るコピーです。
   直しても次の生成で消えます。直すのは `keitai-app/`・`ienaka-app/`・`ienaka-demo/` の側。
3. **このリポジトリは誰でも読めます。** 分配率・契約の中身・個人の連絡先・
   お客様や店舗の実データを、コード・コメント・PR文のどこにも書かないこと。
   （社内文書は非公開リポジトリ `docomo-quote-internal` にあります）
4. **「ドコモ公式」と言わない・金額は概算。** アプリ内文言や資料を作るとき、
   ドコモとの提携を思わせる表現は使えません（利用規約と契約で決めています）。
5. **金額計算（`keitai-app/app.js` の計算部分）とサーバー設定は触らない。**
   誤った金額が店頭に出るのが最悪の事故です。要望として開発元へ依頼してください。

## クラコン側で編集してよい範囲（目安）

| してよい | 例 |
|---|---|
| 文言の修正 | 画面の説明文・ボタン名・ヘルプの吹き出し（`QUOTE_HELP`）の言い回し |
| 説明書の類 | `keitai-app/SUPPORT.md`・`STATS_GUIDE.md`・`TERMS.md` の誤字や表現 |
| 提案・営業資料 | 提案書の文面の相談（ファイル自体は開発元が保管） |

| 開発元に依頼する | 例 |
|---|---|
| 金額・計算・割引 | 料金改定、プラン追加、計算のずれ |
| 保存・同期・ログイン | 「同期しない」「入れない」系はすべて |
| リリース作業 | 版上げ・キャッシュ更新・社内版/配信用の再生成（手順が多く、順番を間違えると壊れるため） |

## 変更が世に出るまでの流れ

```
クラコン側: 修正 → PR を作る（ここまで）
開発元:     内容確認 → テスト → 版上げ → 取り込み → 配信（frontalk.curacon.co.jp へ反映）
```

急ぎのときは PR を作ったうえで開発元へ一報してください。通常は当日〜翌営業日に反映します。

## AIへの頼み方の例

- 「設定画面の『担当者コード』の説明文を、もっと分かりやすい言い回しに直して。PRまで作って」
- 「SUPPORT.md の受付時間の記載を変えたい。10:00〜18:00 のままか確認して、違っていたら直して」
- 「この画面のこのボタンが何をするものか、コードを読んで日本語で説明して」（読むだけの依頼も可）

## 困ったら

手を止めて開発元（安藤）へ。**壊れたかも、と思ったときに一番やってはいけないのは、
続けて直そうとすること**です。PRの段階なら本番には何の影響もありません。
