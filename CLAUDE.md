# 作業ルール

> **このリポジトリを初めて触る場合は、先に `HANDOVER.md`（引き継ぎ指示書）を読んでください。**
> 経緯・現状・やってはいけないことがまとまっています。

## アプリ構成

| パス | 位置づけ |
|---|---|
| `/`（ルート） | ケータイ見積もり **社内利用版**（阪南店の日常業務用・開発はここで行う） |
| `/keitai-app/` | ケータイ見積もり **製品版**（代理店向け販売用・自己完結） |
| `/ienaka/` | イエナカ見積もり **社内利用版**（開発はここで行う） |
| `/ienaka-app/` | イエナカ見積もり **製品版**（代理店向け販売用・自己完結） |
| `/ienaka-tiles/` | タイル式UIの試作（イエナカ製品版のコピー＋`tiles.js`） |
| `/ienaka-demo/` | イエナカ **デモ版**（ログイン不要・オンライン専用。営業のQR配布用。製品版から手動コピーで更新） |

## 修正の反映先（重要）

### ケータイ見積もり

- **明示がない修正依頼は、製品版（`/keitai-app/`）にのみ反映する。**（2026-07-28 以降のルール）
  バージョンを上げるのも `keitai-app/` だけでよい。社内版（ルート）は指示があったときにまとめて同期する。
- **「社内版にも」「両方に」と明示された依頼のときだけ**、ルートにも反映する。
  そのときは `_internal/SYNC_DIFF.md` の「意図的に異なる箇所」を必ず維持すること。
- 社内版だけを直す依頼（ログインまわりなど、社内版にしか無いもの）はルートのみ。

社内版は阪南店が毎日使っているため、**同期が遅れると製品版との差が開く。**
まとまった機能を入れたあとは「社内版にも反映しますか」と一度確認するとよい。

### イエナカ見積もり

- **どちらか明示がない修正依頼は、社内版（`/ienaka/`）にのみ反映する。**
- **「製品版」と明示された依頼は `/ienaka-app/` に反映する。**
- 社内版の内容が固まったら、依頼を受けてから製品版へまとめて同期する。

## 社内版へ同期するとき

社内版と製品版には**意図的に違う箇所**（保存キー・ログインの有無・data.js の中身など）があり、
上書きすると壊れます。同期の指示を受けたときは **`_internal/SYNC_DIFF.md` を読んでから**作業すること。

## リリース手順（共通）

1. 変更後 `node --check` と Playwright で動作確認
2. `APP_VERSION` と `sw.js` の `CACHE` を必ず両方上げる（端末の自動更新に使われる）
3. コミット → `git rebase --onto origin/main HEAD~N` → force-with-lease で push → PR作成 → squashマージ
4. GitHub Pages に新バージョンが出るまで確認してから完了報告する
