# 引き継ぎ指示書（共同開発者向け）

このリポジトリを初めて触る方（およびそのAIアシスタント）が、経緯と現状を把握して
安全に修正を始められるようにまとめたものです。**まず最初にこのファイルを読んでください。**

作業ルール（どこを直すか・リリース手順）は `CLAUDE.md` にあります。あわせて読んでください。

---

## 1. これは何か

ドコモショップの店頭で、料金プラン・オプション・端末代金をその場で見積もるためのWebアプリです。
サーバー側の処理は無く、HTML・CSS・素のJavaScriptだけで動く静的サイトです。
ビルド作業はありません。ファイルを直して push すれば、そのまま公開されます。

- 公開先: GitHub Pages（`main` ブランチへのマージで自動デプロイ・反映まで1〜2分）
- フレームワークなし、npm なし、`node_modules` なし
- 動作確認は Playwright（Chromium）で行っています

## 2. 4つのアプリがあります

| パス | 公開URL | 位置づけ |
|---|---|---|
| `/`（ルート） | `.../docomo-quote/` | **ケータイ見積もり 社内版**。阪南店の日常業務で毎日使用。ここで開発する |
| `/keitai-app/` | `.../docomo-quote/keitai-app/` | **ケータイ見積もり 製品版**。代理店へ販売する自己完結アプリ |
| `/ienaka/` | `.../docomo-quote/ienaka/` | **イエナカ見積もり 社内版**（ドコモ光・ahamo光・home 5G） |
| `/ienaka-app/` | `.../docomo-quote/ienaka-app/` | **イエナカ見積もり 製品版** |
| `/ienaka-tiles/` | `.../docomo-quote/ienaka-tiles/` | タイル式UIの試作。製品版のコピー＋`tiles.js` |

### 触る場所を間違えないでください（最重要）

- **「ケータイ見積もりを直して」→ ルートの `app.js` / `index.html` / `data.js` / `style.css`**
- **「イエナカを直して」→ `/ienaka/`**
- **「製品版を直して」と明示されたときだけ** `/keitai-app/` `/ienaka-app/` を触る

社内版はオーナーが毎日使っているため、依頼の対象がどちらか曖昧なときは**社内版だけ**を直します。
製品版へは、社内版の内容が固まってから、依頼を受けてまとめて反映します。

## 3. ファイルの役割（ケータイ見積もり）

| ファイル | 中身 |
|---|---|
| `index.html` | 画面全体。見積もりタブ・見積書タブ・引き継ぎタブ・マスタ設定タブ |
| `app.js` | 本体（約2,400行）。計算エンジン・描画・帳票・同期がすべてここ |
| `data.js` | 料金マスタの初期値（プラン・オプション・手数料・キャンペーン） |
| `style.css` | 画面と印刷（`@media print`）のスタイル |
| `sw.js` | オフライン用のキャッシュ。**更新の配信に関わるので後述** |

### `app.js` のおおまかな構成

上から順に、料金マスタの読み込み → 見積もり状態（3パターン）→ 同期 → ヘルパー →
**計算エンジン `calcFor()`** → 画面描画 → 引き継ぎシート → 見積書 → マスタ設定タブ →
再計算 `recalc()` → イベント登録 `bindEvents()` → 起動。

金額に関わる修正は、ほぼすべて `calcFor()` の中か、`data.js` のどちらかです。

### 計算の考え方（ここだけは押さえてください）

**期間セグメント方式**です。「キャンペーンが6か月で終わる」「分割が24回で終わる」といった
変化点で期間を区切り、区間ごとの月額を出しています。見積書の「月額の推移」がその出力です。

- 恒常的な項目 → `adhocPerm` に足す
- 期間限定の項目 → `adhocLimited` に `{name, amount, months}` で積む

新しく「○か月間だけ割引」を足したいときは、既存の仕組みに合流させれば段階表示まで自動で出ます。
独自にセグメントを作らないでください。

## 4. これまでの経緯（なぜこうなっているか）

そのまま消すと困る、理由のある実装です。

| 実装 | 理由 |
|---|---|
| 手続き種別・料金プランが「（未選択）」から始まる | 開いた瞬間に金額が入っていると誤案内のもとになるため。未選択のとき事務手数料・頭金は0円 |
| 頭金・事務手数料の自動入力は新規契約・機種変更のときだけ | MNP・プラン変更では店頭で発生しないため |
| 端末代金総額は**頭金を含んだ総額**。分割は「総額 − 頭金」 | 店頭の伝え方に合わせたもの。一括のときは頭金を総額に含めて1行で出す |
| いつでもカエドキは**残価ではなく「23回分の総額（頭金込み）」**を入力 | 店頭で案内する「実質○○円」がそのまま入力値になるため。残価は総額から逆算 |
| 見積書で、通話オプションに含まれる留守番電話・キャッチホンを0円の別行にせず、通話オプションの行にまとめる | 0円の行が並ぶと読みにくいため。MAXの選べる特典も同じ考えで料金プランの行にまとめている |
| 選べる特典で0円になっているオプションは、dカード還元の対象額に**含めない** | 支払いが発生していないため。以前は含めていて還元が過大に出ていた |
| 引き継ぎシートは印刷でA4 1枚に収まるよう、全体を6%縮小している | 項目を最大まで入れても1枚に収めるため。`body.print-staff` に限定してあり、見積書の印刷には影響しない |
| ガスが大阪ガス固定、でんきが関西圏前提 | 阪南店の商圏に合わせたもの。**製品化のときは設定できるようにする必要がある** |

料金は原則としてドコモ公式サイトの記載（一次情報）に合わせています。
ただし**店舗の運用が優先される場合があります**（例: カケホーダイ契約時に留守番電話・キャッチホンを
無料として計算している点は、公式ページに明記が見つからず、店舗の運用に合わせています）。
金額を変えるときは、公式の記載か店舗の運用か、根拠をコミットメッセージに残してください。

## 5. 現状（2026年7月末時点）

### 動いているもの

- 社内版・製品版とも、料金プラン／割引／オプション／端末／初期費用／ポイントの計算
- 3パターン（A・B・C）の同時比較
- 見積書（A4・2ページ）と引き継ぎシート（A4・1ページ）の印刷
- 料金マスタの編集（マスタ設定タブ）
- 製品版の**店舗ログイン（店舗ID＋パスワード）と担当者コード**
- 製品版の**マスタ設定のロック**（開くときに店舗ID＋パスワードを再度求める。管理者だけが料金・担当者を変更できる）
- 製品版のマスタ設定で、**ドコモの商材と店舗独自サービスを分けて表示**（各項目の `own` フラグ。見積もり画面の並びは従来どおり）
- 端末間同期（Firestore）
- 製品版の**料金マスタの履歴**（改定前に戻せる。店舗内の全端末で共有）
- 製品版の**規約・使用許諾・サポート・運用手順**の文書と、アプリ内の「情報」表示

### 販売に向けて残っている課題

1. **社内版の同期先が、オーナー個人のFirebaseプロジェクト（レシピアプリと共用）で、認証がない**
   → 製品版は `stores/{店舗UID}` ＋認証に移行済み。社内版は阪南店専用なのでそのまま
2. **`data.js` に阪南店の独自商材と実売価格が入っている**
   （コネクトα、hozon、店頭あんしんサポート、店頭頭金19,800円など）
   → 製品版はマスタ設定で `own: true` の「店舗独自サービス」として分離済み。
   残りは、出荷時の初期値からこれらを外す（または空にする）判断
3. ~~ガス・でんきが関西圏固定~~ → 当面は関西圏の二次店を狙うため、対応不要と判断
4. 自動テストとCIが無い。金額が壊れても気づけない
5. ~~ライセンス・利用規約・免責が無い~~ → `keitai-app/` に `TERMS.md` `LICENSE.md`
   `SUPPORT.md` `OPERATIONS.md` を用意済み。アプリ内は「情報」ボタンと見積書の注記で対応。
   **〔　〕の箇所を埋めるのは経営判断**（契約単位・価格・支払条件・解約予告・賠償上限・提供元名義）
6. 料金改定を既存の利用者へ配信する手段が無い
   （`localStorage` の保存済みマスタが `data.js` より優先されるため、価格を直しても届かない）
   → 店舗側で戻せるよう「料金マスタの履歴」は入れた。配信そのものは未対応
7. 契約・課金・停止の仕組みが無い（Firebaseコンソールでの手作業。手順は `keitai-app/OPERATIONS.md`）
8. Firestoreの定期バックアップが未設定

## 6. 作業の進め方

### 修正するとき

1. 社内版か製品版かを確認する（§2）
2. 直す
3. `node --check app.js` で構文を確認する
4. **Playwright で実際に動かして確認する**（下記）
5. `APP_VERSION`（`app.js` の先頭）と `sw.js` の `CACHE` を**必ず両方**上げる
6. コミット → PR → squashマージ
7. GitHub Pages に新しい版が出るまで確認してから完了とする

### なぜ `APP_VERSION` と `CACHE` を両方上げるのか

`sw.js` が端末にファイルをキャッシュしています。`CACHE` の名前を変えないと、
**利用者の端末に古いファイルが残り続けます**。`APP_VERSION` は画面下と帳票に出るので、
反映されたかどうかの確認に使います。片方だけ上げると、直したつもりが現場に届きません。

### 動作確認の例

```js
const { chromium } = require('/opt/node22/lib/node_modules/playwright');
(async () => {
  const b = await chromium.launch({ executablePath: '/opt/pw-browsers/chromium' });
  const p = await b.newPage();
  p.on('pageerror', e => console.log('ERR', e.message));   // JSエラーを拾う
  await p.goto('file:///workspace/docomo-quote/index.html');
  await p.waitForTimeout(900);
  await p.selectOption('#planId', { label: 'ドコモ MAX' });
  await p.waitForTimeout(500);
  console.log('月額:', await p.textContent('#sumMonthly'));
  await b.close();
})();
```

`file://` で開くと Service Worker の登録エラーが出ますが、これは無害です。

印刷レイアウトを変えたときは、PDFに出してページ数を必ず確認してください。

```js
await p.emulateMedia({ media: 'print' });
await p.pdf({ path: 'out.pdf', format: 'A4', printBackground: true,
  margin: { top: '10mm', right: '10mm', bottom: '10mm', left: '10mm' } });
// → pdfinfo out.pdf | grep Pages
```

引き継ぎシートを印刷するときは、事前に `document.body.classList.add('print-staff')` が必要です。

### 金額を変えたときは必ず検算する

「月額が◯◯円になった」で終わらせず、内訳を足し算して合っているか確かめてください。
このアプリの価値は金額の正しさそのものです。

## 7. やってはいけないこと

- **社内版の `localStorage` のキー（`dq-master-v3` / `dq-state-v2`）を変える**
  → 阪南店の業務データが読めなくなります
- **製品版のキー（`kq-*`）を社内版と同じにする**
  → 同じドメインに同居しているため、データが混ざります
- 製品版に社内版の旧キーからの移行処理を入れる
  → 阪南店のデータを製品版が引き継いでしまいます
- `main` へ直接 push する（PR経由にしてください）
- `APP_VERSION` だけ上げて `CACHE` を据え置く

## 8. 困ったときの手がかり

- **金額が合わない** → `app.js` の `calcFor()` を読む。`optRows` / `adhocPerm` / `adhocLimited` / `segs` の流れを追う
- **画面に出ない** → `renderXxx()` 系が呼ばれているか。`recalc()` から呼ぶ必要がある描画もあります
- **印刷が2ページになる** → `style.css` の `@media print`。引き継ぎシートは `body.print-staff` の指定
- **同期しない** → 製品版は `firebase-config.js` の `projectId` が空だと同期しません（仕様）
- **リリースしたのに反映されない** → `sw.js` の `CACHE` を上げ忘れていないか
