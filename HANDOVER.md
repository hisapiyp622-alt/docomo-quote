# 引き継ぎ指示書（共同開発者向け）

このリポジトリを初めて触る方（およびそのAIアシスタント）が、経緯と現状を把握して
安全に修正を始められるようにまとめたものです。**まず最初にこのファイルを読んでください。**

作業ルール（どこを直すか・リリース手順）は `CLAUDE.md` にあります。あわせて読んでください。
販売・契約・運用の文書は、非公開リポジトリ `docomo-quote-internal` にあります。
**このリポジトリは公開されています。分配率・営業メモ・契約の中身・連絡先は書かないでください。**

---

## 1. これは何か

ドコモショップの店頭で、料金プラン・オプション・端末代金をその場で見積もるためのWebアプリです。
名前は「**フロントーク**」。サーバー側の処理は無く、HTML・CSS・素のJavaScriptだけで動きます。

- フレームワークなし、npm の依存なし（テストで Playwright を使うだけ）
- 製品版の公開先: **`https://frontalk.curacon.co.jp/`**（配信用リポジトリ `frontalk` から）
- 社内版（阪南店・常盤東店）の公開先: GitHub Pages
- 販売は株式会社Curacon が行い、こちらは開発と保守を持ちます

## 2. 開発する場所は2つだけです（最重要）

| パス | 位置づけ | 触ってよいか |
|---|---|---|
| `keitai-app/` | **ケータイ見積もり（製品版）**。これが本体 | **ここを直す** |
| `ienaka-app/` | イエナカ単体版（出荷はしない。社内版の生成元） | **ここを直す** |
| `/`（ルートの `index.html`・`sw.js`） | ケータイ社内版（阪南店） | **生成物。直接編集しない** |
| `/ienaka/`・`/ienaka-tokiwahigashi/` | イエナカ社内版（阪南・常盤東） | **生成物。直接編集しない** |
| `ienaka-demo/` | 営業のQR配布用デモ（ログイン不要・オンライン専用） | 単体版を直したら同じ変更を入れる |
| `ienaka-tiles/` | タイル式UIの試作（凍結） | 触らない |

**社内版は「製品版と同じコードを読む薄いラッパー」です。**
`node tools/build-internal.js` が作ります。社内版だけの違いはフラグ（`window.KEITAI_INTERNAL` /
`window.IENAKA_INTERNAL`）に集約してあり、**修正は製品版に対して行えば社内版にも自動で効きます。**

出荷用（frontalk）も `node tools/build-product.js <出力先>` が作ります。
**このリポジトリが原本で、配信用リポジトリは直接編集しません。**

### 「どこを直すか」で迷ったら

- 「ケータイ見積もりを直して」→ `keitai-app/`
- 「イエナカを直して」→ `keitai-app/ienaka.js`（ケータイ内蔵）**と** `ienaka-app/`（単体版）の両方
- ルートや `/ienaka/` のファイルに手を入れたくなったら、それは生成元を直すべき合図です

## 3. ファイルの役割（ケータイ見積もり＝ `keitai-app/`）

| ファイル | 中身 |
|---|---|
| `index.html` | 画面全体。見積もり・見積書・引き継ぎ・保存・実績・マスタ設定のタブ |
| `app.js` | 本体。計算エンジン・描画・帳票・同期・配信・契約の判定がすべてここ（1万行超） |
| `data.js` | 配信する料金表の標準値（プラン・オプション・手数料・キャンペーン） |
| `ienaka.js` | 光・home 5G（イエナカ）の計算と画面 |
| `style.css` | 画面と印刷（`@media print`）のスタイル |
| `sw.js` | オフライン用のキャッシュ。**更新の配信に関わるので後述** |
| `firestore.rules` | クラウド（Firestore）の権限。**Firebase コンソールで公開して初めて効きます** |
| `changelog.js` | 更新履歴（アプリ内の「情報」に出る。店舗が読む文章） |

### `app.js` のおおまかな構成

上から順に、端末内保存の入口 → 店舗設定・担当者 → 料金マスタの読み込みと配信 →
見積もり状態（3パターン）→ クラウド同期 → **計算エンジン `calcFor()`** → 画面描画 →
引き継ぎシート → 見積書 → 保存・実績 → マスタ設定タブ → イベント登録 → 起動。

金額に関わる修正は、ほぼすべて `calcFor()` の中か `data.js` のどちらかです。

### 計算の考え方（ここだけは押さえてください）

**期間セグメント方式**です。「キャンペーンが6か月で終わる」「分割が24回で終わる」といった
変化点で期間を区切り、区間ごとの月額を出します。見積書の「月額の推移」がその出力です。

- 恒常的な項目 → `adhocPerm` に足す
- 期間限定の項目 → `adhocLimited` に `{name, amount, months}` で積む

新しく「○か月間だけ割引」を足すときは、この仕組みに合流させれば段階表示まで自動で出ます。
独自にセグメントを作らないでください。

**`calcFor()` に「今日の日付」を持ち込まないでください。**
持ち込むと、過去に作った見積もりを開き直したときに金額が変わり、実績まで狂います。
受付終了（下記）の判定も、画面に出すときだけ行っています。

## 4. 仕組みとして押さえること

### 料金改定の配信（`masterVersion`）

店舗の料金表は端末の中にあり、`data.js` より優先されます。そのままでは価格を直しても届きません。
そこで `data.js` の `masterVersion` を上げて配り、店舗が「料金表の更新があります」を見て適用します。

- 料金を変えたら **`data.js` ＋ `masterVersion` ＋ `updated` の3点セット**（CI が見ています）
- **項目を消してはいけません。**消しても店舗の料金表には残り続け、何も伝わりません
- 受付が終わったものは `retiredFrom`（`"2026-12-01"`）で伝えます。その日から一覧に出なくなり、
  すでに作った見積もり・実績はそのまま残ります

### 契約の器と上位アカウント（クラウド）

- `contracts/{店舗UID}` … 販売側がコンソールで入れる契約状態（`trial` / `active` / `suspended`）。
  お試し期間が切れると書き込みが止まります（データは消えません）
- `roles/{アカウントUID}` … 代理店・エリアの上位アカウント。担当範囲の店舗だけ見られます
- `stores/{店舗UID}` … 店舗情報・料金マスタ・その店舗が使っている版の記録
- **`firestore.rules` を変えたら、Firebase コンソール（またはCLI）で公開するまで効きません**

### オフラインと更新の届き方

`sw.js` が端末にファイルを控えています。`CACHE` の名前を変えないと、
**利用者の端末に古いファイルが残り続けます**。`APP_VERSION` は画面と帳票に出るので、
反映されたかの確認に使います。**片方だけ上げると、直したつもりが現場に届きません。**
（`node tools/release-check.js` がこの食い違いを止めます）

## 5. これまでの経緯（なぜこうなっているか）

そのまま消すと困る、理由のある実装です。

| 実装 | 理由 |
|---|---|
| 手続き種別・料金プランが「（未選択）」から始まる | 開いた瞬間に金額が入っていると誤案内のもとになるため。未選択のとき事務手数料・頭金は0円 |
| 頭金・事務手数料の自動入力は新規契約・機種変更のときだけ | MNP・プラン変更では店頭で発生しないため |
| 端末代金総額は**頭金を含んだ総額**。分割は「総額 − 頭金」 | 店頭の伝え方に合わせたもの |
| いつでもカエドキは**残価ではなく「23回分の総額（頭金込み）」**を入力 | 店頭で案内する「実質○○円」がそのまま入力値になるため |
| 通話オプションに含まれる留守番電話・キャッチホンを0円の別行にしない | 0円の行が並ぶと読みにくいため |
| 選べる特典で0円のオプションは、dカード還元の対象額に**含めない** | 支払いが発生していないため |
| 引き継ぎシートは印刷で6%縮小 | A4 1枚に収めるため。`body.print-staff` に限定 |
| 受付終了の項目を**消さずに印だけ付ける** | 過去の見積もり・保存・実績を壊さないため |
| 選んである項目は、受付終了でも一覧に残す | 開き直したときに黙って消えると、金額が変わって見えるため |

料金は原則としてドコモ公式サイトの記載（一次情報）に合わせています。
ただし**店舗の運用が優先される場合があります**。金額を変えるときは、公式の記載か店舗の運用か、
根拠をコミットメッセージに残してください。

## 6. 現状（2026年9月時点）

### 動いているもの

- 料金プラン／割引／オプション／端末／初期費用／ポイントの計算、3パターン同時比較
- 光・home 5G（イエナカ）の見積もりと、ケータイとの合算
- 見積書・引き継ぎシート・開通までの流れ（A4印刷）
- 保存した見積もり／実績（提案・成約の集計、CSV）
- 端末間同期（Firestore）、料金マスタの履歴（改定前に戻せる）
- 店舗ログイン・担当者コード・マスタ設定のロック
- **料金改定の配信**（`masterVersion`）と**受付終了の配信**（`retiredFrom`）
- **契約の器**（お試し／本契約／停止）と**上位アカウント**（代理店・エリア）
- **保守アカウント**（全店舗の表示・修理）と、店舗一覧に出る「その店舗が使っている版」
- **自動テストとCI**（金額・請求内訳・光・配信・出荷用の並び・Firestoreルール）
- 規約・使用許諾・サポート・実績の使い方の文書と、アプリ内の「情報」表示
- 「調子が悪いときの情報」（端末内の記録をコピーして問い合わせに貼れる）

### 残っている課題

1. **Firestore の定期バックアップが未設定**（Blaze 切替後に設定する。それまでは月1回の手動）
2. **ガス・でんきの会社一覧が関西圏前提**。東海への展開で前提が変わったため、店舗別の設定が要る
3. **店舗の開通が手作業**（Firebase コンソール操作）。店舗数が増えると追いつかない
4. **12月の料金改定**（dカード／ポイ活の計算切り替え）。11月20日に着手する
5. 光の料金が `ienaka.js` の中にあり、配信データになっていない

## 7. 作業の進め方

1. `keitai-app/`（必要なら `ienaka-app/`）を直す
2. `node --check <触ったファイル>` で構文を確認する
3. テストを通す
   ```
   node tests/run-calc-tests.js          # 金額のゴールデン
   node tests/run-bill-tests.js          # 請求内訳の読み取り
   node tests/run-ienaka-tests.js        # 光・5G（統合版と単体版の一致も見る）
   node tests/run-master-update-tests.js # 料金表の配信・受付終了
   node tests/run-product-layout-test.js # 出荷用の並び
   sh tools/test-rules.sh                # firestore.rules を触ったときだけ
   ```
4. **Playwright で実際に動かして確認する**（下記）
5. `keitai-app/app.js` の `APP_VERSION` と `keitai-app/sw.js` の `CACHE` を**必ず両方**上げ、
   `changelog.js` に1件足す（店舗が読む文章で）
6. `node tools/build-internal.js` を実行して、ルート・`/ienaka/` を作り直す
7. コミット → PR → CI が緑になってから squash マージ
8. `node tools/build-product.js` → 配信用リポジトリ `frontalk` へ反映
   （`sh tools/release.sh --ship` でまとめて行えます）
9. 公開されたことを確認してから完了とする

### 動作確認の例

```js
const { chromium } = require('/opt/node22/lib/node_modules/playwright');
(async () => {
  const b = await chromium.launch({ executablePath: '/opt/pw-browsers/chromium' });
  const p = await b.newPage();
  p.on('pageerror', e => console.log('ERR', e.message));   // JSエラーを拾う
  await p.goto('http://127.0.0.1:8000/keitai-app/?kqtest=1');  // 簡易HTTPサーバー越しに開く
  await p.waitForTimeout(900);
  await p.selectOption('#planId', { label: 'ドコモ MAX' });
  console.log('月額:', await p.textContent('#sumMonthly'));
  await b.close();
})();
```

`file://` で開くと Service Worker と同期が動かないため、**簡易HTTPサーバー越しに開いてください**
（テストスクリプトが中で立てているものが参考になります）。
`?kqtest=1` を付けると、検算用の窓口 `window.__KQ_TEST__` が生えます。

印刷レイアウトを変えたときは、PDFに出してページ数を必ず確認してください。

```js
await p.emulateMedia({ media: 'print' });
await p.pdf({ path: 'out.pdf', format: 'A4', printBackground: true,
  margin: { top: '10mm', right: '10mm', bottom: '10mm', left: '10mm' } });
// → pdfinfo out.pdf | grep Pages
```

引き継ぎシートを印刷するときは、事前に `document.body.classList.add('print-staff')` が必要です。

### 金額を変えたときは必ず検算する

「月額が◯◯円になった」で終わらせず、内訳を足し算して合っているか確かめてください。
金額を意図して変えたときは `node tests/run-calc-tests.js --update` でゴールデンを作り直し、
**PRで前後の差を必ず目視してください**。このアプリの価値は金額の正しさそのものです。

## 8. やってはいけないこと

- **生成物を直接編集する**（ルートの `index.html`・`sw.js`、`/ienaka/`・`/ienaka-tokiwahigashi/`）
  → 次の `build-internal` で消えます。直すのは `keitai-app/`・`ienaka-app/`
- **配信用リポジトリ（`frontalk`）を直接編集する** → 同じ理由
- **`data.js` から項目を消す** → 消しても店舗には残り続けます。受付終了は `retiredFrom` で
- **`calcFor()` に今日の日付を持ち込む** → 過去の見積もりの金額と実績が動きます
- **社内版の `localStorage` のキー（`dq-*`）を変える** → 阪南店の業務データが読めなくなります
- **製品版のキー（`kq-*`）を社内版と同じにする** → 同じドメインに同居しているため混ざります
- `main` へ直接 push する（PR経由にしてください）
- `APP_VERSION` だけ上げて `CACHE` を据え置く
- 出荷ファイルに社内の店舗名（阪南など）を書く

## 9. 困ったときの手がかり

- **金額が合わない** → `calcFor()` を読む。`optRows` / `adhocPerm` / `adhocLimited` / `segs` の流れ
- **画面に出ない** → `renderXxx()` 系が呼ばれているか（`recalc()` から呼ぶ描画もあります）
- **印刷が2ページになる** → `style.css` の `@media print`。引き継ぎシートは `body.print-staff`
- **同期しない** → `firebase-config.js` の `projectId` が空だと同期しません（仕様）。
  「同期:権限エラー」が出るときは `firestore.rules` を公開し忘れていないか
- **リリースしたのに反映されない** → `sw.js` の `CACHE` の上げ忘れ（`tools/release-check.js`）
- **店舗で何か起きた** → 店舗に「情報 → 調子が悪いときの情報 → コピー」をお願いする
