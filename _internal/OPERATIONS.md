# 運用手順書（販売側）

代理店へ本アプリを提供・管理するときの手順です。**この文書は社内用です。代理店には渡しません。**

## 契約前に決めておくこと

以下は**あとから変えるのが難しい**ため、最初の1店舗を売る前に決めてください。

**決まったもの**（2026-07-29・安藤さん）

- [x] 契約単位 … **店舗単位**（1契約＝同一住所の1店舗／端末数・担当者数は無制限）
- [x] 支払方法 … **銀行振込**（振込手数料はお客様負担）
- [x] サポートの受付時間 … **10:00〜18:00**（土日祝・年末年始を除く）／応答の目安は翌営業日中
- [x] 提供元の名義 … **株式会社cracon（仮称）**
- [x] 損害賠償 … **「賠償なし」の意向。** ただし故意・重過失まで含めて全部免責とすると条項ごと無効になり、
      かえって上限のない賠償責任が残るため、**故意・重過失を除いて免責＋受領済み利用料金の総額を上限**という
      書き方にしている（`keitai-app/TERMS.md` 6条4・5項）。文字どおりゼロにする場合は要相談

**決まったもの（続き）** — 3文書の空欄はすべて埋まり、代理店に渡せる状態です

- [x] **利用料金**（2026-08-10 決定） … 初期費用 60,000円（税込66,000円）／月額 27,000円（税込29,700円）／
      最低利用期間 6か月（合計 税込244,200円）／期間内解約は残期間分を一括・日割り返金なし／無料お試し1週間
      （`keitai-app/LICENSE.md` 3項・`_internal/APPLICATION_FORM.md` 2項）
- [x] **支払期日・締め日**（2026-08-10 決定） … **月末締め・翌月末払い**
- [x] **窓口のメールアドレス**（2026-08-14 決定） … **w-ogawa@curacon.co.jp**（営業窓口と同じ）。
      TERMS・LICENSE・SUPPORT・PRIVACY・`firebase-config.js` の `KEITAI_VENDOR.contact` に反映済み
- [x] **管轄裁判所**（2026-08-14 決定） … **大阪地方裁判所**（TERMS・販売店契約に記載済み）
- [x] **販売店契約の署名欄の代表取締役氏名**（2026-08-14 決定） … **山口 直** さま（記入済み）
- [x] **制定日**（2026-08-01） … 3文書の末尾
- [x] **法人登記** … 株式会社Curacon（大阪府大阪市淀川区西中島3-8-15 EPO新大阪ビルディング1205）

**補助金（デジタル化・AI導入補助金2026＝旧IT導入補助金）は検討継続**（2026-08-10 時点）。
上の価格は補助金を前提にしていません。使うかどうかは、次の3点が決まってからです。

- [ ] クラコンが**IT導入支援事業者**として事務局に登録するか（法人要件のため開発元単独では不可）
- [ ] クラコンの **gBizIDプライム**の有無（無ければ取得に2〜3週間）
- [ ] 実績報告などの事務負担を**管理費10%の範囲に含めるか**

決まるまでは、営業で「補助金が使えます」と言わないこと。
交付決定前の有償契約はできないため、使う場合の順番は
**無償お試し → 申請 → 交付決定 → 本契約** になります。

> **ONJT（研修系の助成金）は別制度で、要件が重く難しい見込み**です（時間拘束のスケジュール化、
> 全体研修の報告書、クラコンとは別に代理店側で立てる取り組みスケジュールが必要）。
> こちらとデジタル化・AI導入補助金2026を混同しないこと。

**期間・日数は当方の判断で一般的な条件を入れてあります。** 運用に合わなければ変更してください。

| 項目 | 入れた値 | 場所 |
|---|---|---|
| 提供終了の予告 | 3か月前 | TERMS 7条3項 |
| 支払遅延で停止するまで | 30日 | TERMS 8条2項 / LICENSE 7項 |
| 解約の予告 | 1か月前 | TERMS 9条1項 |
| 解約後のデータ削除 | 30日後 | TERMS 9条2項 / LICENSE 6項 |
| 停止中のデータ保持 | 3か月 | LICENSE 7項 |
| 料金改定の予告 | 3か月前 | LICENSE 3項 |

**〔　〕が残ったまま代理店に渡さないでください。**

### 3文書を書くときの注意

- **約束できないことは書かないでください。** 書いた内容がそのまま義務になります（とくに `SUPPORT.md` の応答目安と、料金改定のお知らせ）
- **価格は一度出すと下げられません。** 最初の1店舗に提示する前に決めてください
- 3文書は `keitai-app/` に置いてあり、**アプリの「情報」から誰でも読めます。** 社内向けの注意書きをこの3文書に書かないでください（この運用手順書に書いてください）
- 埋め終えたら、各文書の冒頭にある「本書は制定前の案です」の行を削除してください

## 店舗を追加する

1. [Firebaseコンソール](https://console.firebase.google.com/) → Authentication → Users →「ユーザーを追加」
2. メールアドレス: `店舗ID@keitai-quote.example`（`@` 以降は `keitai-app/firebase-config.js` の `KEITAI_STORE_DOMAIN` と揃える）
3. パスワードを設定（8文字以上・推測しにくいもの）
4. **契約の器に登録する**（下の「契約の器」参照）。お試しの店舗は `status: "trial"` と期限、本契約の店舗は `status: "active"`
5. 店舗IDとパスワードを、店舗の管理者へ**口頭または安全な方法で**伝える
6. 店舗側で `SETUP.md` の手順7（担当者の登録）を実施してもらう

**店舗IDの付け方**は最初に決めて統一してください（例: `hannan01` のように 地名＋連番）。

## 契約の器（お試し・本契約・停止）— 1.100.0 から

契約状態は Firestore の **`contracts/{店舗のUID}`** に持ちます（アプリからは読み取り専用）。
UIDは Authentication → Users の該当行「ユーザーUID」からコピーします。

**登録のしかた**: Firestore Database → データ → `contracts` コレクション（無ければ「コレクションを開始」で作成）→
ドキュメントID に店舗のUIDを貼り付け → フィールドを入れる。

| フィールド | 型 | 値 |
|---|---|---|
| `status` | string | `trial`（お試し）／ `active`（本契約）／ `suspended`（停止） |
| `trialEndsAt` | **timestamp** | お試しの終了日時（開始日＋7日の 23:59 など）。`trial` のときだけ必要 |

アプリ側の動き:

- **trial（期限内）** … 画面上部に「無料お試し期間中（◯月◯日まで・残り◯日）」と申込先が出る
- **trial（期限切れ）** … 「お試し期間が終了しました」の全画面案内が出て使えなくなる。**データは消えない**。
  `active` に変えると、店舗が「ご契約状態を再確認」を押す（またはアプリを開き直す）だけで元どおり使える
- **suspended** … 「ご利用が停止されています」の全画面案内。支払い遅延のときはまずこれを使う（復活は `active` に戻すだけ）
- **ドキュメントが無い店舗** … 従来どおり動く（阪南・常盤東・ogawa01 はこのまま）
- 端末に契約状態の控えを持つため、**オフラインの端末にも期限切れは効きます**。
  Firestoreルール側でも、期限切れ・停止中の店舗からの書き込みは拒否します（ルールの反映が必要・下のチェックリスト2）

**手続きの流れの例**: お試し開始 → `trial`＋期限を登録 → 期限が来ると自動で止まる →
申込書を受領したら `status` を `active` に変える（それだけで再開）→ 支払い遅延30日で `suspended`。

## 利用を停止する（支払い遅延・解約）

1. まず **`contracts/{UID}` の `status` を `suspended`** にする（アプリに案内が出て使えなくなる。復活も一瞬）
2. 解約が確定したら Authentication → Users → 該当ユーザーの行のメニュー →「**ユーザーを無効にする**」
3. 次にアプリを開いた時点でログインできなくなります

**ユーザーを削除しないでください。** 削除するとUIDが変わり、`stores/{UID}` 配下の料金マスタ・見積もりに二度とアクセスできなくなります。復活させる可能性がある間は「無効にする」を使ってください。

## 解約後のデータ削除

保持期間（`keitai-app/TERMS.md` 9条2項）が過ぎたら、Firestore Database で `stores/{該当UID}` のドキュメントを配下ごと削除します。
そのあとで Authentication のユーザーを削除してください。

## パスワードの再設定

Authentication → Users → 該当行のメニュー →「パスワードを再設定」。
アプリ側の「マスタ設定のパスワード」は、店舗の管理者が店舗ID・パスワードで入って決め直せます（`SETUP.md` 参照）。

## 費用の見込み

Firebase の無料枠（Sparkプラン）は、Firestore が 1日あたり読み取り5万・書き込み2万、保存1GiB です。
**数十店舗までは無料枠に収まる想定**です。超えた場合は Blaze プラン（従量課金）に切り替えます。

コンソールの「使用量」で、店舗数が増えたときの推移を月1回は確認してください。

## バックアップ

**現在、Firestore の自動バックアップは設定していません。** 店舗数が増えたら、Blazeプランに切り替えたうえで
定期エクスポート（Cloud Scheduler + Firestore export）の設定を検討してください。

暫定的には、各店舗のマスタ設定にある「現在のマスタ構成をコピー」で控えを取れます。

## リリースの手順

1. `keitai-app/` を変更する
2. `app.js` の `APP_VERSION` と `sw.js` の `CACHE` を**両方**上げる
3. **`changelog.js` の先頭に1件足す**（`v` は上げた `APP_VERSION` と揃える）
4. main にマージすると GitHub Pages へ自動で反映される
5. 各店舗の端末は、次に開いたときに自動でリロードされる

**バージョンを上げ忘れると、端末に古い版が残り続けます。**
**`changelog.js` に足し忘れると、店舗側に「新着」が出ません。** 書く内容は
仕組みの説明ではなく、店舗の方から見て「何ができるようになったか」にしてください。

## 料金改定を各店舗へ配る

ドコモの料金が変わったときの手順です。**店舗のマスタはこちらの `data.js` より優先される**ため、
`data.js` を直すだけでは既存店舗には届きません。版数を上げて知らせる必要があります。

1. `keitai-app/data.js` の該当する金額を直す（一次情報で確認してから）
2. **`masterVersion` を +1 する**（これを忘れると誰にも届きません）
3. `updated` を改定日にする（見積書の「料金データ基準日」に出ます）
4. `app.js` の `APP_VERSION` と `sw.js` の `CACHE` を上げ、`changelog.js` に1件足す
5. main へマージ → GitHub Pages が配信

店舗側では次のようになります。

- マスタ設定の先頭に「料金表の更新があります」と出る
- 担当者コードの画面にも一行出る（マスタ設定を毎日は開かないため）
- 「変わる内容」で金額の前後を確認してから「この内容に更新する」を押す
- 適用前の内容は履歴に残るので、あとから戻せる

### 更新されるもの・されないもの

| 更新される | 更新されない（店舗のもの） |
|---|---|
| 事務手数料 | 店頭頭金の初期値 |
| プランの基本料金・割引・爆アゲ区分・ポイ活上限など | プラン名 |
| 通話オプションの金額 | 店舗が登録したプラン |
| ドコモ商材の金額・選択肢・爆アゲ率・説明 | オプションの名前・置き場所・並び順 |
| 初期費用の定番項目の金額・支払い先 | 店舗独自（`own: true`）にしている項目すべて |
| キャンペーンの期間・対象・割引額 | アクセサリ（すべて店舗の商品） |
| 初期データに増えた新項目（削除済みのものは除く） | 担当者・店舗名・電話番号・テンプレート・保存した見積もり |

**店舗がドコモ商材の金額を自分で変えていた場合は上書きされます。** 意図的に変えていた店舗は、
「変わる内容」を見て気づけますし、適用後に履歴から戻せます。

### 注意

- **`masterVersion` は必ず整数で +1。** 下げたり飛ばしたりしない
- 店舗が「あとで」と放置しても、次に開いたときにまた出ます（消えません）
- 適用は店舗の操作です。**こちらから強制はできません。** 改定日に確実に反映させたい場合は、
  店舗へ連絡して適用してもらってください

## データのバックアップ

守るべきものは2つあり、それぞれ担当が違います。

### 1. 店舗のデータ（店舗が自分で取る）

アプリの **マスタ設定 → バックアップ** から、店舗情報・担当者・料金マスタ・
マスタ履歴・保存した見積もり・テンプレートを1つのJSONファイルに書き出せます。
「バックアップから復元」で戻せます。

- **お客様名は既定で含まれません。** チェックを入れたときだけ含まれます。
  含めたファイルは個人情報を含むので、保管場所に注意するよう店舗に伝えてください
- 復元すると、戻す前の内容が料金マスタの履歴の先頭に残ります
- **導入時の説明に必ず入れてください。** 「月に一度、月初に取る」くらいの案内が現実的です

これは**誤操作からの復旧**に効きます。クラウド同期は「いまの状態を写す」ものなので、
間違えて消した内容は同期先でも消えます。バックアップはそこを埋めるものです。

### 2. Firestore 全体（販売側が取る）

店舗が全部消してしまった、アカウントを間違えて削除した、といった事故に備えるものです。

**Firestore の自動バックアップには Blaze プラン（従量課金）が必要です。**
`keitai-quote` プロジェクトが Spark（無料）のままなら、まず切り替えが要ります。
店舗数が少ないうちは無料枠に収まる見込みですが、課金の有効化そのものは判断事項です。

設定するときの手順:

1. [Firebaseコンソール](https://console.firebase.google.com/) → `keitai-quote` → 歯車 → 使用量と請求 → Blaze へ変更
2. Firestore Database → バックアップ → **バックアップのスケジュールを作成**
3. 推奨: **日次・保持14日**（店舗数が少ないうちはこれで十分です）
4. あわせて **PITR（ポイントインタイム リカバリ）** を有効にすると、直近7日は任意の時点へ戻せます

**Blaze に切り替えるまでの間の代替**として、店舗側のバックアップ（上記1）を
確実に取ってもらうことで、実務上の最悪は避けられます。
ただし**店舗が取っていなければ何も残りません**ので、恒久策にはなりません。

### いつやるか

`_internal/SALES.md` の段取りでは、**最初の1店舗を売る前**に2を用意する想定です。
他社の店舗データを預かった時点で「消えました」は通らないためです。

## 販売開始前のFirebaseチェックリスト（2026-08-08追記）

コード側の対策（ルール強化・保存失敗の警告・復元の検査）は 1.52.0 で反映済み。
以下の3つは **Firebaseコンソールでの手作業** が必要です（アプリの更新では反映されません）。

1. **セルフサインアップの停止（最優先・無料）**
   Authentication → 設定 → ユーザーアクション → 「作成（登録）を許可する」を**オフ**。
   オンのままだと、公開されているAPIキーで第三者がアカウントを作り、Firestoreへ書き込めます。
2. **セキュリティルールの更新（無料）**
   Firestore Database → ルール に `keitai-app/firestore.rules` の最新内容を貼り付けて公開。
   フィールド名の検査と書き込みサイズ上限（900KB）に加え、**契約の器（contracts）の検査**が入ります
   （期限切れ・停止中の店舗の書き込みをサーバー側でも拒否。1.100.0〜）。
   公開後、アプリで「見積もり編集 → 保存 → 別端末で同期」を一度確認すること。
3. **定期バックアップ（Blazeプラン切替後）**
   約款で「解約後30日はデータ保持」を約束しているため、こちら側に控えが必要。
   Blazeに切り替え → `gcloud firestore backups schedules create --database='(default)' --recurrence=daily --retention=7d`
   （または コンソール → Firestore → 災害対策 から日次バックアップを有効化）。
   切替前の暫定運用: 月1回、各店舗に「マスタ設定 → バックアップを保存」を依頼する。
